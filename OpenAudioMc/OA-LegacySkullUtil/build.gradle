plugins {
    id 'java'
}

description = 'OpenAudioMc skull support for legacy servers'

dependencies {
    implementation project(':OpenAudioMc:API')
    implementation libs.nbt.api

    compileOnly libs.paper.api.skull
}
