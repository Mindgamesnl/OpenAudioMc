//import {SimplePeer} from "../../../helpers/libs/SimplePeer";

export class RtcClient {

    constructor(openAudioMc, server, streamKey, micStream) {
        this.openAudioMc = openAudioMc;
        this.server = server;
        this.streamKey = streamKey;


    }

    start() {

    }
}
